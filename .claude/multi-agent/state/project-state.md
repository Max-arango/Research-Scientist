# Project State — OBSERVED

_Generated by Orchestrator during bootstrap. Facts observed, not assumed._

## Environment
- **Target dir:** `/home/fellcrack/Trabajo/Personal/IA-Laboratorio/Claude/Skills/Skills-Developing/Research-Scientist`
- **Repo:** NOT a git repository (`git status` → fatal: not a git repository). Empty dir at bootstrap.
- **Platform:** linux, bash.
- **Runtime available:** Python 3 (assumed present; verified in Phase 2 before writing tests).

## What we are building
A **new Claude Code skill** from scratch: `scientific-research-mae` — a Scientific Research
Operating System (multi-agent, human-in-the-loop, evidence-driven).

## Paradigm decision (ADR-001)
Markdown-driven agent contracts (like the `multi-agent-engineering` skill this is modeled on)
+ JSON Schemas for scientific objects + a lightweight **Python `core/`** for machine-checkable
state (IDs, storage, provenance graph, evidence engine, snapshots, validation) + tests.
Rationale: orchestration is performed by Claude reading contracts (real), while reproducibility
demands machine-verifiable state (real). No fake event-loop pretending to "run agents".

## Build/test commands (this deliverable)
- Tests: `python3 -m pytest scientific-research-mae/tests/ -q` (or `python3 -m unittest`).
- No package manager / build step (pure-stdlib Python + markdown). No new dependencies.

## Policy (effective, from SKILL default gates)
- CRITICAL → never ship. HIGH → never without documented human override. MEDIUM → mitigate or
  documented risk acceptance. LOW → document. INFO → informational.
- max_iterations: 6 · stall_threshold: 2 · max_fix_attempts: 3.
