# Production Decision — PD-001

## Header
- task_id: TASK-001
- title: Audit and test scientific-research-mae skill vs original spec
- iteration: 1
- decided_by: orchestrator
- decided_at: 2026-09-03
- status: **APPROVED-WITH-MINOR**

## Decision
**APPROVED** for the `scientific-research-mae` skill.

The skill substantially implements the 34-section spec. The cognitive layer (agent
contracts) and the machine layer (`srmae` stdlib core) together enforce the spine
SOURCE → CLAIM → HYPOTHESIS → PROTOCOL → EXPERIMENT → RESULT → ANALYSIS →
CRITIQUE → VERDICT, with provenance, HITL, safety, evidence engine, snapshots,
disputes, failures, and metrics.

## Evidence
- Tests: 51/51 pass (`python3 -m unittest discover -s tests -q` → "Ran 51 tests in 0.078s, OK").
- Demo: end-to-end green (`examples/demo_research.py` exit 0; 5 HITL approvals, 3
  independent replications, 1 critique, 1 verdict, snapshot restore verified,
  `unsupported_claim_rate=0.0`, `traceability=1.0`, `human_approval_count=5`).
- Spec mapping: see `decisions/ADR-001-audit-of-sr-mae.md` (all 34 sections
  mapped to file:line evidence).

## Blockers
None.

## Findings
| ID | Severity | Status | Action |
|---|---|---|---|
| FINDING-001 (multi-agent/ empty skeleton) | MEDIUM | RESOLVED | populated: 7 agents + SKILL + policy + templates + smoke tests |
| FINDING-002 (1/7 benchmark cases) | LOW | ACCEPTED | defer until user asks for replication/adversarial-only flows |
| FINDING-003 (schemas not enforced by code) | LOW | ACCEPTED | cognitive layer + tests cover it; jsonschema would break stdlib-only |
| FINDING-004 (Communicator no machine renderer) | LOW | ACCEPTED | by design — Communicator is cognitive-only |
| FINDING-005 (all tests + demo pass) | INFO | VERIFIED | positive evidence |
| FINDING-006 (no git repo) | INFO | RESOLVED | environment property, not skill property |

## Recommended next actions
1. (optional) Add 3 more benchmark demos to satisfy spec §32 fully (replication,
   adversarial-only, failure-heavy).
2. (optional) Wire jsonschema-style required-key validation into `new_object()`.
3. (optional) Add a machine-renderer for the Communicator.

## Alternatives considered
- REJECTED — unjustified; core behavior is sound, demos pass, agent contracts enforce spec.
- HUMAN_REQUIRED — unjustified; no CRITICAL/HIGH blocker, no irreversible op.

## Sign-off
The skill is fit for use. The two MEDIUM/LOW items above are quality-of-life, not correctness.